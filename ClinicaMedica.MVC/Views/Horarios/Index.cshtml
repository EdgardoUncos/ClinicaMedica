@model IEnumerable<ClinicaMedica.Model.DTOs.Basic.HorariosDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HorarioId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioFin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioFin)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.HorarioId }) |
                @Html.ActionLink("Details", "Details", new { id=item.HorarioId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.HorarioId})
            </td>
        </tr>
}
    </tbody>
</table>
