@model IEnumerable<ClinicaMedica.Model.DTOs.Basic.MedicosDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Persona.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especialidades.Detalle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EspecialidadId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sueldo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Persona.Apellido)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Especialidades.Detalle)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.EspecialidadId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sueldo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MedicoId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.MedicoId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.MedicoId })
            </td>
        </tr>
}
    </tbody>
</table>
